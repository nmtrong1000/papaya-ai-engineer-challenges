# PC-07. Verify & polish responsive layout

## Requirement

The card layout is verified at key breakpoints and any overflow, clipping, or spacing issues are fixed.

## Approach

The `flex flex-col md:flex-row` structure from PC-03 already handles the layout switch. This task covers testing at standard viewport widths, fixing any card content that overflows or truncates on narrow screens, and ensuring equal card heights on desktop. No structural changes to the component tree — only Tailwind class adjustments on existing elements.

## Execution Steps

- [x] Open DevTools and test at 375 px (iPhone SE), 768 px (tablet), 1280 px (desktop)
- [x] Added `p-4 md:p-6` responsive padding and `max-w-5xl mx-auto` container
- [x] Added `min-w-0` to card to allow proper flex shrinking without content overflow
- [x] Equal card heights confirmed via default `items-stretch` on flex container
- [x] Lint and build pass with no errors

## How to Test

```bash
npm run dev
# Open http://localhost:5173
# Chrome DevTools → device toolbar → test 375px, 768px, 1280px
```

Expected result:
- 375 px: cards stack vertically, full-width, readable, no horizontal scroll
- 768 px: transition point — either layout is acceptable, no broken state
- 1280 px: three equal-height cards side by side

## Time

- **In:** 2026-06-18 22:34:22
- **Out:** 2026-06-18 22:44:35
- **Estimate:** 15 min
