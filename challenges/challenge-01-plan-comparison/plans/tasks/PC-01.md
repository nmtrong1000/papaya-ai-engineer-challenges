# PC-01. Set up project scaffolding & Tailwind

## Requirement

A runnable React + Vite + TypeScript project with Tailwind CSS configured exists at `challenges/challenge-01-plan-comparison/app/`.

## Approach

Scaffold with `npm create vite` using the `react-ts` template, then install and configure Tailwind CSS v3 via its PostCSS plugin. The app directory lives inside the challenge directory to keep it self-contained. Verify the dev server renders the default Vite page before moving on.

## Execution Steps

- [x] Run `npm create vite@latest app -- --template react-ts` inside `challenges/challenge-01-plan-comparison/`
- [x] `cd app && npm install`
- [x] Install Tailwind: Tailwind v4 was installed — used `@tailwindcss/vite` plugin instead of PostCSS config (v4 changed setup)
- [x] Configure `vite.config.ts` to use `tailwindcss()` from `@tailwindcss/vite`
- [x] Replace `src/index.css` with `@import "tailwindcss"` (v4 syntax)
- [x] Confirmed `src/main.tsx` imports `./index.css`
- [x] Cleared `src/App.tsx` to a minimal `<h1>Hello</h1>` component
- [x] Run `npm run build` — exits 0, no TypeScript or Tailwind errors

## How to Test

```bash
cd challenges/challenge-01-plan-comparison/app
npm run dev
# Open http://localhost:5173 — page renders "Hello" with no console errors
```

```bash
npm run build
# Build completes with no TypeScript or Tailwind errors
```

Expected result: dev server starts, "Hello" renders, `npm run build` exits 0.

## Notes

- Tailwind v4.3.1 was installed (latest). v4 drops `tailwind.config.js` and `npx tailwindcss init`; configuration is CSS-first via `@import "tailwindcss"` and the `@tailwindcss/vite` plugin.

## Time

- **In:** 2026-06-18 18:41:52
- **Out:** 2026-06-18 18:47:30
- **Estimate:** 15 min
